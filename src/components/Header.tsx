import React from 'react';
import DateRange from './DateRange';

const Header = () => {
  return (
    <header>
      <h2>Header</h2>
      <DateRange />
    </header>
  );
};

export default Header;
